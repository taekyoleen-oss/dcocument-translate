[phases.setup]
nixPkgs = ["python311"]
aptPkgs = ["fonts-nanum"]

[phases.install]
cmds = ["python -m venv /opt/venv", ". /opt/venv/bin/activate", "pip install -r requirements.txt"]

[start]
cmd = "cd web && python -m uvicorn app:app --host 0.0.0.0 --port $PORT"
