[phases.setup]
nixPkgs = ["python311", "nanum-gothic-fonts"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[start]
cmd = "cd web && python -m uvicorn app:app --host 0.0.0.0 --port $PORT"
