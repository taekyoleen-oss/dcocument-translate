# Fonts

Place `NanumGothic.ttf` in this directory before running `compose_pdf.py`.

Download: https://fonts.google.com/specimen/Nanum+Gothic

The PDF composer expects the font at `fonts/NanumGothic.ttf` relative to the
project root, or pass a custom path via the `--font` argument.
